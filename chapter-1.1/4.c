#include<stdio.h>

int main()
{
    int exam_count;

    scanf("%d", &exam_count); //inputs number of total exams
    
    printf("%d\n", exam_count); //as pen and exam count would be the same

    return 0;
}